# Copy this file to `.env` and customize as needed.

AWS_REGION=
AWS_ACCOUNT_ID=

# デプロイするイメージタグ（通常は HEAD コミットハッシュ）
# 未設定の場合: export IMAGE_TAG=$(git rev-parse HEAD)
IMAGE_TAG=

# Web イメージビルド引数: クライアントバンドルに焼き込まれる API のベース URL
# DevApiStack の API ALB DNS 名、またはカスタムドメインを指定
NEXT_PUBLIC_API_URL=http://
