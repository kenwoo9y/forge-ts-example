# 開発用DB（forge_ts_dev）とは別にテスト専用DBを用意すること
# （Integrationテストは対象テーブルをTRUNCATEするため、開発用DBを直接指定すると開発データが消える）。
DB_HOST=postgres
DB_PORT=5432
DB_NAME=<POSTGRES_TEST_DB>
DB_USERNAME=<POSTGRES_USER>
DB_PASSWORD=<POSTGRES_PASSWORD>
JWT_SECRET="your-secret-key"
LOG_LEVEL=silent
