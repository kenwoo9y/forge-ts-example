API_URL=http://localhost:3000
# Any random string you choose — different from the API's JWT_SECRET
AUTH_SECRET="your-secret-key"

# For E2E tests — pick any values, then create a matching user in the DB before running the tests
E2E_USERNAME=your_test_user
E2E_PASSWORD=your_password